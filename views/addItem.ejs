<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Dazzle & Co.</title>
  </head>
  <body>
    <%- include('_nav', { info: info, userId: userId }) %>
    <div class="max-w-7xl mx-auto px-4 py-12">
      <div class="flex justify-between items-center mb-10">
        <h1 class="header text-3xl font-normal">Add an Item</h1>
      </div>
      <form method="POST" action="/dashboard/add">
        <div class="mb-6">
          <label class="block mb-2 text-sm font-medium text-gray-900"
            >Title</label
          >
          <input
            type="text"
            name="title"
            class="w-full p-4 border border-gray-200 text-sm focus:outline-none block"
            placeholder="Item Title"
          />
          <% if (errors) { %>
          <p class="text-sm text-red-500 mt-4">
            <%= errors.find(el => el.includes("Title")); %>
          </p>
          <% } %>
        </div>
        <div class="mb-6">
          <label class="block mb-2 text-sm font-medium text-gray-900"
            >Description</label
          >
          <textarea
            id="message"
            rows="4"
            name="description"
            class="w-full p-4 border border-gray-200 text-sm focus:outline-none block"
            placeholder="Type Item description..."
          ></textarea>
          <% if (errors) { %>
          <p class="text-sm text-red-500 mt-4">
            <%= errors.find(el => el.includes("Description")); %>
          </p>
          <% } %>
        </div>
        <div class="mb-6">
          <label class="block mb-2 text-sm font-medium text-gray-900"
            >Category</label
          >
          <select
            name="CategoryId"
            class="w-full p-4 border border-gray-200 text-sm focus:outline-none block"
          >
            <option selected value="">-- Select --</option>
            <% categories.forEach(element => { %>
            <option value="<%= element.id %>"><%= element.name %></option>

            <% }) %>
          </select>
          <% if (errors) { %>
          <p class="text-sm text-red-500 mt-4">
            <%= errors.find(el => el.includes("Category")); %>
          </p>
          <% } %>
        </div>
        <div class="mb-6">
          <label class="block mb-2 text-sm font-medium text-gray-900"
            >Price</label
          >
          <input
            type="number"
            name="price"
            class="w-full p-4 border border-gray-200 text-sm focus:outline-none block"
            placeholder="Item price"
          />
          <% if (errors) { %>
          <p class="text-sm text-red-500 mt-4">
            <%= errors.find(el => el.includes("Price")); %>
          </p>
          <% } %>
        </div>
        <div class="mb-6">
          <label class="block mb-2 text-sm font-medium text-gray-900"
            >Stock</label
          >
          <input
            type="number"
            name="stock"
            class="w-full p-4 border border-gray-200 text-sm focus:outline-none block"
            placeholder="Item stock"
          />
          <% if (errors) { %>
          <p class="text-sm text-red-500 mt-4">
            <%= errors.find(el => el.includes("Stock")); %>
          </p>
          <% } %>
        </div>
        <div class="mb-6">
          <label class="block mb-2 text-sm font-medium text-gray-900"
            >Photo URL</label
          >
          <input
            type="text"
            name="photoURL"
            class="w-full p-4 border border-gray-200 text-sm focus:outline-none block"
            placeholder="Photo URL"
          />
          <% if (errors) { %>
          <p class="text-sm text-red-500 mt-4">
            <%= errors.find(el => el.includes("Photo")); %>
          </p>
          <% } %>
        </div>

        <button
          type="submit"
          class="w-full bg-black text-white py-3 font-medium text-sm"
        >
          Submit
        </button>
      </form>
    </div>
  </body>
</html>
